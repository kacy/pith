to_lower('Ärger')  = Ärger
fold('Ärger')      = ärger
to_lower('ПРИВЕТ') = ПРИВЕТ
fold('ПРИВЕТ')     = привет

to_lower says 'Ärger' == 'ärger': false
fold says the same:               true

'ΣΣ' and 'σς' are the same word
  lowercased equal: false
  folded equal:     true

both render as: é and é
  raw bytes equal:  false
  normalized equal: true

eq_fold_normalized('E' + accent, 'é'): true

fold('ß') = ß  (simple folding never changes length)
so 'straße' and 'STRASSE' do not compare equal: false
