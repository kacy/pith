struct Point:
    pub x: Int
    pub y: Int

impl Point:
    fn translate(dx: Int, dy: Int) -> Point:
        return Point(self.x + dx, self.y + dy)

fn main():
    point := Point(3, 4)
    translated_point := point.translate(1, 2)
