# helper: a string-valued map owned here, reassigned by the importer
pub mut registry: Map[String, String] := {}

pub fn owner_read(k: String) -> String:
    return registry.get_default(k, "MISSING")
