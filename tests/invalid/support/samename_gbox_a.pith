# one of two same-named generic structs for samename_generic_ctor_foreign:
# this module's Box is the only one that declares only_a().
pub struct Box[T]:
    pub v: T

impl Box:
    fn only_a() -> String:
        return "a-only"
