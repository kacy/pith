enum Shape:
    Circle(Float)
    Point

fn main():
    a := Shape.Circle(1.0, 2.0)
    b := Shape.Circle("wide")
    good := Shape.Circle(1.0)
    match good:
        Shape.Circle(x, y) => print("bad arity")
        _ => print("other")
