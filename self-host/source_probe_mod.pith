mut source := ""
mut current_position := 0

pub fn init_source(src: String):
    source = src
    current_position = 0

pub fn source_len() -> Int:
    return source.len()

pub fn first_char() -> String:
    if current_position >= source.len():
        return ""
    return source[current_position]
