# helper for test_transitive_field_offset: defines a struct with two string
# fields. another module returns lists of it, and the top-level test reads its
# fields without importing this module directly.

pub struct Pair:
    pub name: String
    pub value: String

pub fn pair(name: String, value: String) -> Pair:
    return Pair(name, value)
