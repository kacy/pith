# helper for test_xmod_float: float math that the caller imports from
# another module. an older codegen bug dropped float returns to 0 across a
# module boundary, so these need to survive the call intact.
pub fn scale(x: Float) -> Float:
    return x * 2.5

pub fn half(x: Float) -> Float:
    return x / 2.0
