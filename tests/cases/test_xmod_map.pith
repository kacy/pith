# regression: reading map values built in another module used to return
# empty. the values now read back correctly.
from xmod_map_helper import build

fn main():
    m := build()
    print("a={m["a"]} b={m["b"]} c={m["c"]}")
    print("size={m.len()}")
