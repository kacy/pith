# `data[0]` on an empty Bytes aborts with the bytes out-of-bounds diagnostic.
import std.bytes as bytes

fn main():
    data := bytes.buffer().take_bytes()
    print("len:{data.len()}")
    print("after:{data[0]}")
