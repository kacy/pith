fn main():
    text := "Alpha: Beta"
    part := text.substring(0, 5)
    print(part)
    trimmed := text.substring(6, text.len()).trim()
    print(trimmed)
    lower := text.substring(0, 5).trim().to_lower()
    print(lower)
    parts := text.split(":")
    print(parts.len().to_string())
