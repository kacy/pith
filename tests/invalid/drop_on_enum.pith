# an enum releases tagged payloads rather than fields; only a struct can
# implement Drop.
enum Shape:
    Circle(Float)
    Square(Float)

impl Drop for Shape:
    fn drop():
        print("gone")

fn main():
    s := Shape.Circle(1.0)
    print("made")
