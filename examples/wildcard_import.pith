# Wildcard imports - import all public items from a module
# Usage: from module import *

from std.math import * 

fn main():
    print("=== Wildcard Import Demo ===")
    print("")

    # All public functions from std.math are available
    a := abs(-42)
    print("abs(-42) = {a}")

    b := min(10, 5)
    print("min(10, 5) = {b}")

    c := max(10, 5)
    print("max(10, 5) = {c}")

    d := clamp(15, 0, 10)
    print("clamp(15, 0, 10) = {d}")

    e := sqrt(25.0)
    print("sqrt(25.0) = {e}")

    f := pow(2.0, 10.0)
    print("pow(2.0, 10.0) = {f}")

    print("")
    print("Wildcard imports work!")
