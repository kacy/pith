import std.bytes as bytes

# a struct with a heap Bytes field, constructed from another module in
# test_module_ctor_heap_field.
pub struct Payload:
    pub code: Int
    pub label: String
    pub data: Bytes
