# pipe (|) operator

fn double(x: Int) -> Int:
    return x * 2

fn to_string(x: Int) -> String:
    return "{x}"

fn main():
    # pipe: thread a value through a single function
    result := 10 | double

    # chained pipes
    msg := 10 | double | to_string
