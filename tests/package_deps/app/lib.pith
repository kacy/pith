# package_app - imports a local path dependency.

from greeter import greet
import greeter.extra as extra

fn main():
    print(greet("agent"))
    print(extra.double(21).to_string())

test "local dependency imports resolve":
    assert_eq(greet("agent"), "hello, agent")
    assert_eq(extra.double(21), 42)
