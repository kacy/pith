# one of two same-named structs used by test_samename_method_dispatch: both
# declare a `label` method, and only the receiver's own declaration may answer.
pub struct Widget:
    pub tag: String

impl Widget:
    fn label() -> String:
        return "one:" + self.tag

pub fn make_one() -> Widget:
    return Widget("alpha")
