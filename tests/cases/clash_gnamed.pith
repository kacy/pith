# the interface both clash modules implement on their own same-named generic.
# it lives in a module of its own so neither clash module depends on the
# other: the two Holder declarations stay as independent as the two Box ones.
pub interface Named:
    fn name() -> String
