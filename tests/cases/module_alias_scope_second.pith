# helper for test_module_alias_scope. `only_here` exists in this module alone,
# so the alias for the first helper must not be able to see it.

pub fn label() -> String:
    return "second"

pub fn only_here() -> String:
    return "second only"
