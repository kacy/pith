# clash_layout_b — same name Box, a different and longer field layout, also built
# with named fields.
pub struct Box:
    pub count: Int
    pub kind: String
    pub note: String

pub fn make_b() -> Box:
    return Box(count: 222, kind: "beta", note: "gamma")
