# helper for leak_cross_module_struct_return: the struct's defining module,
# which the leak case never imports.

pub struct Pair:
    pub name: String
    pub value: String

pub fn pair(name: String, value: String) -> Pair:
    return Pair(name, value)
