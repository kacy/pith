# tuples

fn swap(a: Int, b: Int) -> (Int, Int):
    return (b, a)

fn main():
    pair := (1, "hello")
    number_value := pair.0
    name := pair.1
    swapped := swap(10, 20)
    print("tuples work")
