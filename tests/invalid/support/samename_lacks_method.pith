# support module for samename_method_foreign: a same-named struct with a
# different layout and no refresh() of its own.
pub struct Gadget:
    pub s: String

pub fn make_lacks() -> Gadget:
    return Gadget("only a string")
