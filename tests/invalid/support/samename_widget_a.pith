# one of two same-named structs for samename_struct_ctor_foreign: this module's
# Widget is the only one that declares only_a().
pub struct Widget:
    pub n: Int

impl Widget:
    fn only_a() -> String:
        return "a-only"
