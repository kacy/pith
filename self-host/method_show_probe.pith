struct Point:
    pub x: Int
    pub y: Int

interface Display:
    fn show(x: String) -> String

impl Display for Point:
    fn show(x: String) -> String:
        return x

fn main():
    point := Point(3, 4)
    display_result := point.show("hello")
    print(display_result)
