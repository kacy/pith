# an impl of Drop that never declares drop() has nothing for the destructor
# to call.
struct Slot:
    pub id: Int

impl Drop for Slot:
    fn close():
        print("closed")

fn main():
    s := Slot(1)
    print("{s.id}")
