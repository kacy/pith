# importing a name its module never declares must fail at check time,
# not die later as a silent unknown call in the backend
from support.phantom_import_helper_mod import real_helper, imaginary_helper

fn main():
    print(real_helper().to_string())
