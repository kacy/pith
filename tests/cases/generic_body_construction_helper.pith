# helper for test_generic_body_construction: a plain struct another module
# declares, so a construction of it inside a generic body is spelled
# `shapes.Remote(...)` and takes the module-qualified lowering path.

pub struct Remote:
    pub label: String
    pub parts: List[String]
