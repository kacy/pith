# helper for test_module_struct_construction: pub structs built by name from
# another module through an import alias.

pub struct Point:
    pub x: Int
    pub y: Int

pub struct Record:
    pub name: String
    pub count: Int
    pub on: Bool
