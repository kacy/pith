# regression: Set codegen used to fail the verifier. a literal Set, add, and
# the contains/len queries all lower cleanly now.
fn main():
    mut s: Set[Int] := {1, 2, 3}
    s.add(4)
    s.add(2)
    print("has 2: {s.contains(2)}")
    print("has 9: {s.contains(9)}")
    print("size: {s.len()}")
