# helper for test_module_const_access: pub module-level constants of a few
# types, referenced from another module through an import alias.

pub SMALL := 0
pub LARGE := 4096
pub LABEL := "h2"
pub ENABLED := true
