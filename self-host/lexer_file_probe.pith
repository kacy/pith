from lexer import lex_all

fn main():
    src := read_file("/tmp/global_list_len_probe.pith")
    tokens := lex_all(src)
    print(tokens.len().to_string())
    mut i := 0
    while i < tokens.len():
        tok := tokens[i]
        print(tok.kind + ":" + tok.value)
        i = i + 1
