import std.encoding as encoding
import std.bytes as bytes

# doubles a number the slow way,
# for demonstration purposes
fn double(n: Int) -> Int:
    return n + n

fn main():
    print(double(3).to_string())
    print(encoding.to_hex(bytes.from_string_utf8("a")))
