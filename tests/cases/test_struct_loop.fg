struct Item:
    name: String
    count: Int

fn main():
    mut items: List[Item] := []
    items.push(Item("apple", 3))
    items.push(Item("banana", 5))
    print(items.len().to_string())
    for item in items:
        print(item.name + ": " + item.count.to_string())
