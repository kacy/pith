# regression: float values returned from an imported function used to come
# back as 0. they now survive the module boundary.
from xmod_float_helper import scale, half

fn main():
    print("scale(4.0)={scale(4.0)}")
    print("half(9.0)={half(9.0)}")
    print("chained={half(scale(4.0))}")
