from scope import reset_scope_arena, create_scope, define_binding, lookup_binding

fn main():
    reset_scope_arena()
    _root := create_scope(-1)
    define_binding(0, "xs", 18, false)
    print(lookup_binding(0, "xs").to_string())
