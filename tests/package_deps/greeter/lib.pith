# greeter - local package dependency fixture.

# Return a greeting for a caller.
pub fn greet(name: String) -> String:
    return "hello, " + name

test "greet formats the name":
    assert_eq(greet("agent"), "hello, agent")
