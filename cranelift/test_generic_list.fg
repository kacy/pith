fn first[T](list: List[T]) -> T:
    return list[0]

fn main():
    let numbers := [10, 20, 30]
    let first_num := first[Int](numbers)
    print_int(first_num)
    
    let words := ["hello", "world"]
    let first_word := first[String](words)
    print(first_word)