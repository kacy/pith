# the int-keyed twin of map_fused_update_missing_key: a fused update through
# the integer entry point aborts on a key the map does not hold.
fn main():
    mut m: Map[Int, Int] := {}
    m.insert(1, 1)
    print("len:{m.len()}")
    m.insert(2, m[2] + 1)
    print("after:{m[2]}")
