fn may_fail(x: Int) -> Int!:
    if x < 0:
        fail "negative number"
    return x * 2

fn caller(x: Int) -> Int!:
    let result := may_fail(x)!
    return result + 10

fn main():
    let success_result := caller(5)
    print_int(success_result)
    print("Success path works!")