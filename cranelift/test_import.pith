from self-host/types import TypeInfo, ti_primitive, add_type_info, get_type_info

fn test_types() -> Int:
    let tid := add_type_info(ti_primitive("Int"))
    print_int(tid)
    return tid

fn main():
    let result := test_types()
    print_int(result)
    print("Types imported successfully!")