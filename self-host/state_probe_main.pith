from state_probe_mod import set_g, get_g_len, clear_items, push_item, item_count

fn main():
    set_g("abc")
    print(get_g_len().to_string())
    clear_items()
    push_item("x")
    print(item_count().to_string())
