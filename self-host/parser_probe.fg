from lexer import lex_all
from parser_boot import parse

fn main():
    src := read_file("/tmp/global_list_len_probe.fg")
    tokens := lex_all(src)
    root := parse(tokens)
    print(root.to_string())
