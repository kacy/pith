interface Iterator[T]:
    fn next() -> T?

struct NotIter:
    val: Int

fn to_list[I: Iterator[T], T](it: I) -> List[T]:
    mut out: List[T] := []
    return out

fn main():
    xs := to_list(NotIter(1))
    print("{xs.len()}")
