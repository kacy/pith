from self-host/ast import add_node, add_leaf, reset_arena
from self-host/types import ti_primitive, add_type_info

fn test_ast():
    reset_arena()
    let node_idx := add_node("test", "value", [], 1, 1)
    print_int(node_idx)

fn main():
    test_ast()
    print("Import test successful!")