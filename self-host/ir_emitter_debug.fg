# ir_emitter_debug.fg — thin debug wrapper over the main IR emitter
#
# keep the debug entrypoint around for probes, but do not maintain a second
# copy of the lowering logic here.

from ir_emitter import emit_ir
