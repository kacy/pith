# helper module for test_generic_bare_import_split: a generic imported by
# name and called bare from the test.
pub fn show[T](v: T) -> String:
    return "<{v}>"

pub fn same[T](a: T, b: T) -> Bool:
    return a == b
