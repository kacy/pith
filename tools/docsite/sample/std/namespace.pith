# std.namespace — a module that exports nothing
#
# std.crypto is shaped like this: a header pointing at the submodules, with no
# declarations of its own. it must still render as a module with a summary.
