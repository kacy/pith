fn helper(x: Int) -> Int:
    return x + 1

fn apply(x: Int, f: fn(Int) -> Int) -> Int:
    return f(x)

fn make_fn(v: Int) -> fn() -> Int:
    return fn() => helper(v)

fn main():
    f := make_fn(4)
    print(f().to_string())
    y := 10
    print(apply(5, fn(x: Int) => x + y).to_string())
