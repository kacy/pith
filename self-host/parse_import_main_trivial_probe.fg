from lexer import lex_all
from parser import parse
from ast import reset_arena

fn main() -> String!:
    src := read_file("self-host/import_main_trivial_src.fg")!
    token_list := lex_all(src)
    reset_arena()
    parsed_root := parse(token_list)
    if parsed_root >= 0:
        print("done")
    return ""
