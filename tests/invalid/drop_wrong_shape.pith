# the impl must declare exactly fn drop(): no parameters, no return value,
# and nothing else beside it.
struct Slot:
    pub id: Int

impl Drop for Slot:
    fn drop(force: Bool) -> Int:
        return self.id

    fn close():
        print("closed")

fn main():
    s := Slot(1)
    print("{s.id}")
