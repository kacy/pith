import std.binary as binary
import std.bytes as bytes

fn reader_position(r: binary.Reader) -> Int:
    return binary.position(r)

fn main() -> Int:
    data := bytes.from_string_utf8("abc")
    r := binary.reader(data)
    print("pos:" + reader_position(r).to_string())
    return 0
