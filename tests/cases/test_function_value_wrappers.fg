fn app(x: Int) -> Int:
    return x + 1

fn apply(x: Int, f: fn(Int) -> Int) -> Int:
    return f(x)

fn wrap(f: fn(Int) -> Int) -> fn(Int) -> Int:
    return fn(x: Int) => f(x) + 1

fn main():
    print("apply:{apply(5, app)}")
    wrapped := wrap(app)
    print("wrapped:{wrapped(5)}")
