# `xs[i]` with a negative index aborts with the list out-of-bounds diagnostic.
fn main():
    xs: List[Int] := [7, 9]
    mut i := 1
    print("before:{xs[i]}")
    i = i - 2
    print("after:{xs[i]}")
