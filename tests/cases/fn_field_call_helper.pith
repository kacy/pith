# helper module for fn_value_field_calls: a struct with a function-valued
# field built in another module.
pub struct Ops:
    pub add: fn(Int, Int) -> Int

pub fn plus(a: Int, b: Int) -> Int:
    return a + b

pub fn make() -> Ops:
    return Ops(plus)
