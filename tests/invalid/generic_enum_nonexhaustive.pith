# a generic enum's match is under the exhaustiveness check like any other
# enum: covering only Some must report the missing Nothing.
enum Opt[T]:
    Some(T)
    Nothing

fn main():
    x := Opt.Some(3)
    match x:
        Opt.Some(n) => print("{n}")
