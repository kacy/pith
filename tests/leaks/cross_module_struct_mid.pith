# helper for leak_cross_module_struct_return: returns a Pair built in
# cross_module_struct_inner, so the leak case reaches the type only through
# this return type.

import cross_module_struct_inner as inner

pub fn one(i: Int, text: String) -> inner.Pair:
    return inner.pair("k{i}", text)
