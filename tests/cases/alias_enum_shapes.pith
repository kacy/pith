# helper module for test_alias_enum_variant: one boxed enum (a payload
# variant exists) and one unboxed enum, both constructed through the
# importing file's module alias.
pub enum Phase:
    Zed(String)
    Alpha

pub enum Dir:
    North
    South
