# a generic struct has one destructor per instance, with no place to splice a
# drop call, so it cannot implement Drop.
struct Guard[T]:
    pub v: T

impl Drop for Guard:
    fn drop():
        print("gone")

fn main():
    g := Guard[Int](v: 1)
    print("{g.v}")
