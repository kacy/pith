# comments and declarations
import std.io as io
from std.collections import List, Map

pub fn greet(name: String) -> String:
    return "hello, {name}!"

fn main():
    mut retries: Int = 3
    if retries > 0 and true:
        retries -= 1
    else:
        fail "no retries left"
