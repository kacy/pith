from scope import reset_scope_arena, create_scope, define_binding, lookup_binding

fn main():
    reset_scope_arena()
    root := create_scope(-1)
    define_binding(root, "x", 7, false)
    print(lookup_binding(root, "x").to_string())
