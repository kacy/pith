from lexer import lex_all

fn main():
    tokens := lex_all("mut xs")
    print(tokens.len().to_string())
    if tokens.len() > 0:
        print(tokens[0].kind)
    if tokens.len() > 1:
        print(tokens[1].kind)
