# the other same-named generic struct for samename_generic_ctor_foreign: no
# only_a() here, so calling it on this module's Box must be E209.
pub struct Box[T]:
    pub v: T

impl Box:
    fn only_b() -> String:
        return "b-only"
