# a call with the wrong number of arguments inside a generic body, reported
# where it sits once the body is checked per specialization.

fn pair(a: Int, b: Int) -> Int:
    return a + b

fn apply[T](v: T, n: Int) -> Int:
    return pair(n)

fn main():
    print("{apply("x", 2)}")
