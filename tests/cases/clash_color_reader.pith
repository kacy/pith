# imports a's Color and matches it; b's Color is also pulled into the graph by
# the test root, so the match must resolve to a's Color, not b's.
from clash_color_a import a_pick, Color

pub fn describe_a() -> String:
    match a_pick():
        Color.Red => "red"
        Color.Blue => "blue"
