from std.testing import assert_eq, assert_ne, done

fn main():
    assert_eq(1 + 1, 2)
    assert_eq("forge".to_upper(), "FORGE")
    assert_ne(7, 8)
    assert_eq(false, true)
    assert_ne("same", "same")
    done()
