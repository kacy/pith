{
  "DisabledTests": {
    "Basic-Client-TLS-Sync*": "TLS 1.2 session resumption is not implemented by design (resumption is 1.3-only)",
    "Basic-Client-NoTicket-TLS-Sync*": "TLS 1.2 session resumption is not implemented by design (resumption is 1.3-only)",
    "Basic-Client-RenewTicket*": "TLS 1.2 session resumption is not implemented by design (resumption is 1.3-only)",
    "Basic-Server-RSA-TLS*": "static RSA key exchange is refused by design (ECDHE-only fallback)",
    "*-TLS1": "TLS 1.0 is not supported by design (the stack's floor is 1.2)",
    "*-TLS11": "TLS 1.1 is not supported by design (the stack's floor is 1.2)",
    "TrailingMessageData-CertificateRequest-TLS": "the 1.2 client refuses client-certificate authentication before parsing the request body, so the decode error the test expects is never reached",
    "RSAKeyUsage-Client-WantEncipherment-*": "static RSA key exchange is refused by design, so an encipherment-only certificate never gets far enough to be judged",
    "Resume-Server-OmitAllPSKsOnSecondClientHello": "a second ClientHello only follows a HelloRetryRequest, which this stack does not negotiate",
    "ResumeTLS12SessionID-TLS13": "TLS 1.2 session-id resumption is not implemented by design",
    "WrongMessageType-NewSessionTicket-TLS": "the 1.2 client never offers the session_ticket extension, so the server's mangled ticket is never sent",
    "TrailingMessageData-NewSessionTicket-TLS": "the 1.2 client never offers the session_ticket extension, so the server's mangled ticket is never sent",
    "TrailingDataWithFinished-Resume-Client-TLS12": "TLS 1.2 session resumption is not implemented by design, so the mangled resumption flight is never reached",
    "ClientAuth-*-Client-TLS-Sync*": "TLS 1.2 client-certificate authentication is refused by design",
    "ClientAuth-SignatureType*": "the client signs rsa-pss-sha256 and ecdsa-p256-sha256 only; these cases require sha-384 schemes",
    "ClientAuth-NoFallback-*": "the client signs rsa-pss-sha256 and ecdsa-p256-sha256 only; these cases require sha-384 and sha-1 schemes",
    "*-TLS1-TLS": "TLS 1.0 is not supported by design (the stack's floor is 1.2)",
    "*-TLS11-TLS": "TLS 1.1 is not supported by design (the stack's floor is 1.2)",
    "Resume-*-TLS12-TLS12-*": "TLS 1.2 session resumption is not implemented by design",
    "UnsolicitedServerNameAck-*": "the shim always dials with a server name, so the ack it sends is never actually unsolicited from this client's view",
    "*-SignDefault-Ed25519-*": "ed25519 handshake signing is not implemented",
    "*-SignDefault-ML-DSA-*": "ML-DSA signing is not implemented",
    "*-SignDefault-ECDSA_P384_SHA384-*": "ecdsa p-384 server/client certificates are not configured (the p-384 signing primitive exists; no cert fixture)",
    "*-SignDefault-ECDSA_P521_SHA512-*": "ecdsa p-521 signing is not implemented",
    "*-SignDefault-*SHA1*": "sha-1 signatures are not produced by policy",
    "*-SignDefault-RSA_PKCS1_SHA256_LEGACY-*": "the legacy pkcs1 code point is not used for handshake signatures",
    "Client-SignDefault-*-TLS12": "TLS 1.2 client-certificate authentication is refused by design",
    "Client-SignDefault-RSA_PKCS1_*-TLS13": "rsa_pkcs1_* names a certificate signature only, never a 1.3 handshake signature (rfc 8446 \u00a74.2.3), so a request offering only pkcs1 is declined",
    "ConflictingVersionNegotiation": "expects the server to negotiate TLS 1.1 from supported_versions; pre-1.2 protocols are not spoken",
    "NoCommonCurves": "expects a fallback to the static-RSA key exchange, which this stack does not serve",
    "Downgrade-TLS11-*": "the runner's pre-1.2 endpoint needs a CBC suite to reach the downgrade-sentinel exchange; none are served",
    "Downgrade-TLS1-*": "the runner's pre-1.2 endpoint needs a CBC suite to reach the downgrade-sentinel exchange; none are served",
    "TrustAnchors-*": "the trust_anchors draft extension is not implemented; unknown extensions are ignored per rfc 8446 \u00a74.2",
    "ExtensionTrailingData-TrustAnchors-*": "the trust_anchors draft extension is not implemented",
    "CertificateCipherMismatch-*": "expects per-cipher certificate-type selection between multiple certificates; this server carries one certificate per config",
    "HelloRetryRequest-DuplicateCurve-TLS13": "forces a retry onto the P-384 key-exchange group, which this stack does not serve (x25519 and P-256 shares are always offered)",
    "HelloRetryRequest-Cookie-Curve-TLS13": "forces a retry onto the P-384 key-exchange group, which this stack does not serve (x25519 and P-256 shares are always offered)",
    "SecondHelloRetryRequest-TLS13": "forces a retry onto the P-384 key-exchange group, which this stack does not serve (x25519 and P-256 shares are always offered)",
    "TLS13-HRR-InvalidCompressionMethod": "forces a retry onto the P-384 key-exchange group, which this stack does not serve (x25519 and P-256 shares are always offered)",
    "PartialServerHelloWithHelloRetryRequest": "forces a retry onto the P-384 key-exchange group, which this stack does not serve (x25519 and P-256 shares are always offered)"
  }
}
