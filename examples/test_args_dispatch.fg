fn main():
    a := args()
    print(a.len())
    if a.len() >= 2:
        cmd := a[1]
        print(cmd)
        if cmd == "hello":
            print("matched hello")
        elif cmd == "world":
            print("matched world")
        else:
            print("no match")
