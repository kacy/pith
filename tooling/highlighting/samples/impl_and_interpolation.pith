struct Point:
    pub x: Int
    pub y: Int

impl Point:
    fn format(self: Point) -> String:
        return "({self.x}, {self.y})"

fn log_point(p: Point):
    msg := p.format()
    print("point: {msg}")
