# the other same-named struct for samename_struct_ctor_foreign: no only_a()
# here, so calling it on this module's Widget must be E209.
pub struct Widget:
    pub n: Int

impl Widget:
    fn only_b() -> String:
        return "b-only"
