# types.fg — type representation for the self-hosted type checker
#
# mirrors src/types.zig but uses forge idioms: flat struct with string
# kind tag instead of tagged union, global lists instead of pointer-based
# tables. TypeIds are plain integers indexing into a flat list.

# ---------------------------------------------------------------
# type id constants — must match the zig bootstrap
# ---------------------------------------------------------------

pub TID_INT := 0
pub TID_UINT := 1
pub TID_FLOAT := 2
pub TID_BOOL := 3
pub TID_STRING := 4
pub TID_BYTES := 5
pub TID_VOID := 6
pub TID_INT8 := 7
pub TID_INT16 := 8
pub TID_INT32 := 9
pub TID_INT64 := 10
pub TID_UINT8 := 11
pub TID_UINT16 := 12
pub TID_UINT32 := 13
pub TID_UINT64 := 14
pub TID_ERR := 15
pub TID_FIRST_USER := 16

# ---------------------------------------------------------------
# type info — flat struct with string kind tag
# ---------------------------------------------------------------
#
# kind values: "primitive", "struct", "enum", "function", "optional",
#   "result", "tuple", "task", "channel", "list", "map", "set"
#
# unused fields default to empty/0/-1 depending on kind.

pub struct TypeInfo:
    pub kind: String
    pub name: String
    # struct fields
    pub fields: List[String]
    pub field_types: List[Int]
    pub field_pub: List[Bool]
    pub field_mut: List[Bool]
    # function signature
    pub param_types: List[Int]
    pub return_type: Int
    # optional/list/set/task/channel inner type
    pub inner: Int
    # map key/value types
    pub key_type: Int
    pub value_type: Int
    # tuple element types
    pub elements: List[Int]
    # enum variants
    pub variants: List[String]
    pub variant_types: List[List[Int]]

# ---------------------------------------------------------------
# constructors — one per kind for clarity
# ---------------------------------------------------------------

pub fn ti_primitive(name: String) -> TypeInfo:
    mut empty_s: List[String] := []
    mut empty_i: List[Int] := []
    mut empty_b: List[Bool] := []
    mut empty_li: List[List[Int]] := []
    return TypeInfo("primitive", name, empty_s, empty_i, empty_b, empty_b, empty_i, -1, -1, -1, -1, empty_i, empty_s, empty_li)

pub fn ti_struct(name: String, fields: List[String], field_types: List[Int], field_pub: List[Bool], field_mut: List[Bool]) -> TypeInfo:
    mut empty_i: List[Int] := []
    mut empty_s: List[String] := []
    mut empty_li: List[List[Int]] := []
    return TypeInfo("struct", name, fields, field_types, field_pub, field_mut, empty_i, -1, -1, -1, -1, empty_i, empty_s, empty_li)

pub fn ti_enum(name: String, variants: List[String], variant_types: List[List[Int]]) -> TypeInfo:
    mut empty_s: List[String] := []
    mut empty_i: List[Int] := []
    mut empty_b: List[Bool] := []
    mut empty_li: List[List[Int]] := []
    return TypeInfo("enum", name, empty_s, empty_i, empty_b, empty_b, empty_i, -1, -1, -1, -1, empty_i, variants, variant_types)

pub fn ti_function(param_types: List[Int], return_type: Int) -> TypeInfo:
    mut empty_s: List[String] := []
    mut empty_i: List[Int] := []
    mut empty_b: List[Bool] := []
    mut empty_li: List[List[Int]] := []
    return TypeInfo("function", "", empty_s, empty_i, empty_b, empty_b, param_types, return_type, -1, -1, -1, empty_i, empty_s, empty_li)

pub fn ti_optional(inner: Int) -> TypeInfo:
    mut empty_s: List[String] := []
    mut empty_i: List[Int] := []
    mut empty_b: List[Bool] := []
    mut empty_li: List[List[Int]] := []
    return TypeInfo("optional", "", empty_s, empty_i, empty_b, empty_b, empty_i, -1, inner, -1, -1, empty_i, empty_s, empty_li)

pub fn ti_result(ok_type: Int, err_type: Int) -> TypeInfo:
    mut empty_s: List[String] := []
    mut empty_i: List[Int] := []
    mut empty_b: List[Bool] := []
    mut empty_li: List[List[Int]] := []
    return TypeInfo("result", "", empty_s, empty_i, empty_b, empty_b, empty_i, -1, ok_type, -1, err_type, empty_i, empty_s, empty_li)

pub fn ti_tuple(elements: List[Int]) -> TypeInfo:
    mut empty_s: List[String] := []
    mut empty_i: List[Int] := []
    mut empty_b: List[Bool] := []
    mut empty_li: List[List[Int]] := []
    return TypeInfo("tuple", "", empty_s, empty_i, empty_b, empty_b, empty_i, -1, -1, -1, -1, elements, empty_s, empty_li)

pub fn ti_list(element: Int) -> TypeInfo:
    mut empty_s: List[String] := []
    mut empty_i: List[Int] := []
    mut empty_b: List[Bool] := []
    mut empty_li: List[List[Int]] := []
    return TypeInfo("list", "", empty_s, empty_i, empty_b, empty_b, empty_i, -1, element, -1, -1, empty_i, empty_s, empty_li)

pub fn ti_map(key_type: Int, val_type: Int) -> TypeInfo:
    mut empty_s: List[String] := []
    mut empty_i: List[Int] := []
    mut empty_b: List[Bool] := []
    mut empty_li: List[List[Int]] := []
    return TypeInfo("map", "", empty_s, empty_i, empty_b, empty_b, empty_i, -1, -1, key_type, val_type, empty_i, empty_s, empty_li)

pub fn ti_set(element: Int) -> TypeInfo:
    mut empty_s: List[String] := []
    mut empty_i: List[Int] := []
    mut empty_b: List[Bool] := []
    mut empty_li: List[List[Int]] := []
    return TypeInfo("set", "", empty_s, empty_i, empty_b, empty_b, empty_i, -1, element, -1, -1, empty_i, empty_s, empty_li)

pub fn ti_task(inner: Int) -> TypeInfo:
    mut empty_s: List[String] := []
    mut empty_i: List[Int] := []
    mut empty_b: List[Bool] := []
    mut empty_li: List[List[Int]] := []
    return TypeInfo("task", "", empty_s, empty_i, empty_b, empty_b, empty_i, -1, inner, -1, -1, empty_i, empty_s, empty_li)

pub fn ti_channel(inner: Int) -> TypeInfo:
    mut empty_s: List[String] := []
    mut empty_i: List[Int] := []
    mut empty_b: List[Bool] := []
    mut empty_li: List[List[Int]] := []
    return TypeInfo("channel", "", empty_s, empty_i, empty_b, empty_b, empty_i, -1, inner, -1, -1, empty_i, empty_s, empty_li)

# ---------------------------------------------------------------
# type table — global flat list + name map
# ---------------------------------------------------------------

mut tt_types: List[TypeInfo] := []
mut tt_names: Map[String, Int] := {}

# add a type to the table, return its id
pub fn tt_add(info: TypeInfo) -> Int:
    tt_types.push(info)
    return tt_types.len() - 1

# register a name -> type id mapping
pub fn tt_register(name: String, tid: Int):
    tt_names.insert(name, tid)

# look up a type by name, returns -1 if not found
pub fn tt_lookup(name: String) -> Int:
    if tt_names.contains_key(name):
        return tt_names[name]
    return -1

# get the number of types in the table
pub fn tt_count() -> Int:
    return tt_types.len()

# get type info by id
pub fn tt_get(tid: Int) -> TypeInfo:
    return tt_types[tid]

# get the human-readable name for a type id
pub fn tt_type_name(tid: Int) -> String:
    if tid == TID_INT:
        return "Int"
    if tid == TID_UINT:
        return "UInt"
    if tid == TID_FLOAT:
        return "Float"
    if tid == TID_BOOL:
        return "Bool"
    if tid == TID_STRING:
        return "String"
    if tid == TID_BYTES:
        return "Bytes"
    if tid == TID_VOID:
        return "Void"
    if tid == TID_INT8:
        return "Int8"
    if tid == TID_INT16:
        return "Int16"
    if tid == TID_INT32:
        return "Int32"
    if tid == TID_INT64:
        return "Int64"
    if tid == TID_UINT8:
        return "UInt8"
    if tid == TID_UINT16:
        return "UInt16"
    if tid == TID_UINT32:
        return "UInt32"
    if tid == TID_UINT64:
        return "UInt64"
    if tid == TID_ERR:
        return "<error>"
    if tid < 0:
        return "<unknown>"
    if tid >= tt_types.len():
        return "<unknown>"
    info := tt_types[tid]
    if info.kind == "struct":
        return info.name
    if info.kind == "enum":
        return info.name
    if info.kind == "primitive":
        return info.name
    if info.kind == "function":
        return "fn"
    if info.kind == "optional":
        return "optional"
    if info.kind == "result":
        return "result"
    if info.kind == "tuple":
        return "tuple"
    if info.kind == "task":
        return "Task"
    if info.kind == "channel":
        return "Channel"
    if info.kind == "list":
        return "List"
    if info.kind == "map":
        return "Map"
    if info.kind == "set":
        return "Set"
    return "<unknown>"

# ---------------------------------------------------------------
# helper predicates
# ---------------------------------------------------------------

pub fn tt_is_numeric(tid: Int) -> Bool:
    if tid == TID_INT:
        return true
    if tid == TID_UINT:
        return true
    if tid == TID_FLOAT:
        return true
    if tid == TID_INT8:
        return true
    if tid == TID_INT16:
        return true
    if tid == TID_INT32:
        return true
    if tid == TID_INT64:
        return true
    if tid == TID_UINT8:
        return true
    if tid == TID_UINT16:
        return true
    if tid == TID_UINT32:
        return true
    if tid == TID_UINT64:
        return true
    return false

pub fn tt_is_integer(tid: Int) -> Bool:
    if tid == TID_INT:
        return true
    if tid == TID_UINT:
        return true
    if tid == TID_INT8:
        return true
    if tid == TID_INT16:
        return true
    if tid == TID_INT32:
        return true
    if tid == TID_INT64:
        return true
    if tid == TID_UINT8:
        return true
    if tid == TID_UINT16:
        return true
    if tid == TID_UINT32:
        return true
    if tid == TID_UINT64:
        return true
    return false

pub fn tt_is_err(tid: Int) -> Bool:
    return tid == TID_ERR

# ---------------------------------------------------------------
# initialization — register all 16 builtin types
# ---------------------------------------------------------------

pub fn tt_init():
    tt_types = []
    tt_names = {}
    # primitives at known indices 0-14
    tt_add(ti_primitive("Int"))
    tt_register("Int", TID_INT)
    tt_add(ti_primitive("UInt"))
    tt_register("UInt", TID_UINT)
    tt_add(ti_primitive("Float"))
    tt_register("Float", TID_FLOAT)
    tt_add(ti_primitive("Bool"))
    tt_register("Bool", TID_BOOL)
    tt_add(ti_primitive("String"))
    tt_register("String", TID_STRING)
    tt_add(ti_primitive("Bytes"))
    tt_register("Bytes", TID_BYTES)
    tt_add(ti_primitive("Void"))
    tt_register("Void", TID_VOID)
    tt_add(ti_primitive("Int8"))
    tt_register("Int8", TID_INT8)
    tt_add(ti_primitive("Int16"))
    tt_register("Int16", TID_INT16)
    tt_add(ti_primitive("Int32"))
    tt_register("Int32", TID_INT32)
    tt_add(ti_primitive("Int64"))
    tt_register("Int64", TID_INT64)
    tt_add(ti_primitive("UInt8"))
    tt_register("UInt8", TID_UINT8)
    tt_add(ti_primitive("UInt16"))
    tt_register("UInt16", TID_UINT16)
    tt_add(ti_primitive("UInt32"))
    tt_register("UInt32", TID_UINT32)
    tt_add(ti_primitive("UInt64"))
    tt_register("UInt64", TID_UINT64)
    # error sentinel at index 15
    tt_add(ti_primitive("<error>"))
    tt_register("<error>", TID_ERR)
