import samename_alpha as alpha
import samename_beta as beta
fn main():
    a := alpha.boxes(4)
    b := beta.boxes(["x", "y", "z"])
    print("sum=" + alpha.sum(a).to_string() + " joined=" + beta.joined(b))
