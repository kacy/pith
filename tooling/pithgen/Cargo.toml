[package]
name = "pithgen"
version = "0.1.0"
edition = "2021"

# standalone on purpose: the compiler workspace stays untouched
[workspace]

[[bin]]
name = "pithgen"
path = "src/main.rs"

[profile.release]
opt-level = 2
