# a payload-free generic enum constructor bound without an annotation has
# no instance to infer, and used to stay silently error-typed — a later
# match on the binding ran no arm at runtime. the checker now demands the
# annotation at the binding.
enum Opt[T]:
    Some(T)
    Nothing

fn main():
    x := Opt.Nothing
    print("built")
