--- fall-through ---
fallthrough body
fallthrough defer counter=1
--- return ---
early return
return defer counter=2
--- lifo (second runs first) ---
lifo body
second registered counter=3
first registered counter=4
--- untaken branch (x=0) ---
after if
--- taken branch (x=1) ---
inside if
untaken defer counter=5
after if
--- defer before an early return (x=1) ---
early exit
before-return defer counter=6
--- defer after the return point (x=0) ---
past the return
after-return defer counter=7
before-return defer counter=8
