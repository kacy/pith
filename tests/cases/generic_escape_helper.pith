# keeps what it is handed, so a caller's local that escapes into another
# module through an argument can be read back after the caller's frame is
# gone (see test_generic_body_escape_release).

pub mut kept: List[String] := []

pub fn keep(s: String):
    kept.push(s)
