struct Item:
    name: String
    count: Int

fn main():
    mut items: List[Item] := []
    items.push(Item("apple", 3))
    items.push(Item("banana", 5))
    print(items.len())
    for item in items:
        print(item.name + ": " + to_string(item.count))
